name: CI/CD → GCP Cloud Run

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  PROJECT_ID:   ${{ secrets.GCP_PROJECT_ID }}
  REGION:       us-central1
  SERVICE_NAME: mental-health-sentiment
  REGISTRY:     us-central1-docker.pkg.dev
  REPO:         mental-health-sentiment-repo

jobs:
  test:
    name: 🧪 Run Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.10", cache: pip }
      - run: pip install -r requirements.txt pytest
      - run: pytest tests/ -v --tb=short
        env: { MODEL_DIR: tests/mock_models }

  build-push:
    name: 🐳 Build & Push Image
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    outputs:
      image_uri: ${{ steps.set_image.outputs.image_uri }}
    steps:
      - uses: actions/checkout@v4

      - uses: google-github-actions/auth@v2
        with:
          credentials_json: "${{ secrets.GCP_SA_KEY }}"

      - uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker
        run: gcloud auth configure-docker us-central1-docker.pkg.dev --quiet

      - name: Set image URI
        id: set_image
        run: |
          IMAGE="us-central1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/mental-health-sentiment-repo/mental-health-sentiment"
          echo "image_uri=${IMAGE}" >> $GITHUB_OUTPUT
          echo "Image URI: ${IMAGE}"

      - name: Build and push
        run: |
          IMAGE="${{ steps.set_image.outputs.image_uri }}"
          SHA=$(echo "${{ github.sha }}" | cut -c1-7)
          docker build -t "${IMAGE}:${SHA}" -t "${IMAGE}:latest" .
          docker push "${IMAGE}:${SHA}"
          docker push "${IMAGE}:latest"

  deploy:
    name: 🚀 Deploy to Cloud Run
    runs-on: ubuntu-latest
    needs: build-push
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - uses: google-github-actions/auth@v2
        with:
          credentials_json: "${{ secrets.GCP_SA_KEY }}"

      - uses: google-github-actions/setup-gcloud@v2

      - name: Deploy to Cloud Run
        run: |
          IMAGE="${{ needs.build-push.outputs.image_uri }}:latest"
          echo "Deploying image: ${IMAGE}"
          gcloud run deploy mental-health-sentiment \
            --image "${IMAGE}" \
            --region us-central1 \
            --platform managed \
            --allow-unauthenticated \
            --memory 2Gi \
            --cpu 2 \
            --min-instances 0 \
            --max-instances 3 \
            --timeout 120 \
            --set-env-vars "GCS_BUCKET=${{ secrets.GCS_BUCKET }},MODEL_DIR=/app/models,MAX_LEN=100" \
            --service-account "${{ secrets.CLOUD_RUN_SA_EMAIL }}"

      - name: Get service URL
        run: |
          URL=$(gcloud run services describe mental-health-sentiment \
            --region us-central1 \
            --format 'value(status.url)')
          echo "✅ Deployed to: ${URL}"
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" "${URL}/health")
          echo "Health check: ${STATUS}"
          [ "$STATUS" = "200" ] && echo "✅ Health check passed!" || echo "⚠️ Health check returned ${STATUS}"
